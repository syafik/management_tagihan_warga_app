<div class="row">
  <div class="col-md-12 grid-margin stretch-card">
    <div class="card">
      <div class="card-body">
        <div class="d-sm-flex align-items-center mb-4">
          <h4 class="card-title mb-sm-0">Daftar Cash Flows</h4>
          <% if current_user.is_admin? %>
            <a href="<%= new_notification_path %>" class="btn btn-primary ml-auto mb-3 mb-sm-0"> Buat Notification Baru</a>
          <% end %>
        </div>
        <%= render partial: 'shared/search_form',
                   locals: {
                       url: search_notifications_path,
                       attributes: Notification.ransackable_attributes,
                       predicates: Notification.ransack_predicates
                   } %>
        <div id="content-data">
          <%= render partial: 'data' %>
        </div>
      </div>
    </div>
  </div>
</div>
