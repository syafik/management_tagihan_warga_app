<div class="w-full max-w-7xl mx-auto px-2 sm:px-6 lg:px-8 py-2 sm:py-6 overflow-x-hidden">
  <!-- Header -->
  <div class="bg-gradient-to-r from-white to-gray-50 shadow-sm rounded-xl mb-3 sm:mb-6 border border-gray-100">
    <div class="px-3 py-3 sm:px-6 sm:py-5">
      <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-4">
        <div class="flex-1 min-w-0">
          <div class="flex items-center space-x-2 sm:space-x-3 mb-2 sm:mb-3">
            <div class="w-8 h-8 sm:w-10 sm:h-10 bg-gradient-to-br from-orange-500 to-red-500 rounded-xl flex items-center justify-center shadow-sm flex-shrink-0">
              <svg class="w-5 h-5 sm:w-6 sm:h-6 text-white" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
              </svg>
            </div>
            <div class="min-w-0">
              <h1 class="text-lg sm:text-2xl font-bold text-gray-900 truncate">
                <% if current_user.is_warga? %>
                  Informasi Tunggakan
                <% else %>
                  Daftar Tunggakan Iuran
                <% end %>
              </h1>
              <p class="text-xs sm:text-sm text-gray-500 mt-0.5 truncate">
                Data per <%= @cutoff_date.strftime('%d %B %Y') %>
              </p>
            </div>
          </div>

          <div class="flex flex-wrap items-center gap-1.5 sm:gap-2 text-xs sm:text-sm">
            <div class="inline-flex items-center px-2 sm:px-3 py-1 bg-blue-50 text-blue-700 rounded-lg border border-blue-200">
              <svg class="w-3 h-3 sm:w-4 sm:h-4 mr-1 sm:mr-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
              </svg>
              <span class="font-medium text-xs sm:text-sm"><%= @start_date.strftime('%b %y') %> - <%= @cutoff_date.strftime('%b %y') %></span>
            </div>
            <div class="inline-flex items-center px-2 sm:px-3 py-1 bg-amber-50 text-amber-700 rounded-lg border border-amber-200">
              <svg class="w-3 h-3 sm:w-4 sm:h-4 mr-1 sm:mr-1.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"/>
              </svg>
              <span class="font-medium text-xs sm:text-sm">Min <%= @threshold %> bln</span>
            </div>
          </div>
        </div>

        <% unless current_user.is_warga? %>
          <div class="flex-shrink-0">
            <div class="bg-gradient-to-br from-red-500 to-rose-600 rounded-xl px-3 py-2 sm:px-4 sm:py-3 shadow-sm">
              <div class="text-xs font-medium text-red-100 mb-0.5 sm:mb-1">Total Rumah</div>
              <div class="text-xl sm:text-2xl font-bold text-white"><%= @total_arrears_count %></div>
              <div class="text-xs text-red-100">Menunggak</div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <% if @arrears_data.empty? %>
    <!-- Empty State -->
    <div class="bg-white shadow rounded-lg">
      <div class="text-center py-12">
        <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
        </svg>
        <h3 class="mt-2 text-sm font-medium text-gray-900">Tidak Ada Tunggakan</h3>
        <p class="mt-1 text-sm text-gray-500">Tidak ada rumah dengan tunggakan ≥ <%= @threshold %> bulan.</p>
      </div>
    </div>
  <% else %>
    <!-- Desktop Table View (hidden on mobile) -->
    <div class="hidden md:block bg-white shadow rounded-lg overflow-hidden">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              Blok Rumah
            </th>
            <% if current_user.is_warga? %>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                Nama
              </th>
            <% else %>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                Kepala Keluarga & Kontak
              </th>
              <th scope="col" class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">
                Bayar 2025
              </th>
              <th scope="col" class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">
                Tunggakan Lama
              </th>
              <th scope="col" class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">
                Total Bulan Tunggakan
              </th>
              <th scope="col" class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">
                Status
              </th>
            <% end %>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% @arrears_data.each do |data| %>
            <tr class="hover:bg-gray-50">
              <td class="px-6 py-4 whitespace-nowrap">
                <% if current_user.is_warga? %>
                  <div class="text-2xl font-bold text-gray-900">
                    <%= data[:address].block_address.upcase %>
                  </div>
                <% else %>
                  <div class="flex items-center">
                    <div class="flex-shrink-0 h-10 w-10">
                      <div class="h-10 w-10 rounded-full bg-indigo-100 flex items-center justify-center">
                        <span class="text-indigo-600 font-medium text-sm">
                          <%= data[:address].block_address.gsub(/[^A-Za-z]/, '').upcase %>
                        </span>
                      </div>
                    </div>
                    <div class="ml-4">
                      <div class="text-sm font-medium text-gray-900">
                        <%= data[:address].block_address.upcase %>
                      </div>
                    </div>
                  </div>
                <% end %>
              </td>
              <% if current_user.is_warga? %>
                <td class="px-6 py-4 whitespace-nowrap">
                  <div class="text-sm text-gray-900">
                    <%= data[:head_of_family]&.name || '-' %>
                  </div>
                </td>
              <% else %>
                <td class="px-6 py-4 whitespace-nowrap">
                  <div class="text-sm font-medium text-gray-900">
                    <%= data[:head_of_family]&.name || '-' %>
                  </div>
                  <% if data[:head_of_family]&.phone_number.present? %>
                    <div class="text-sm text-gray-500">
                      <%= data[:head_of_family].phone_number %>
                    </div>
                  <% end %>
                  <% if data[:residents].count > 1 %>
                    <div class="text-xs text-gray-500 mt-1">
                      + <%= data[:residents].count - 1 %> anggota keluarga
                    </div>
                  <% end %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-center">
                  <div class="text-sm text-gray-900"><%= data[:total_paid_2025] %> / <%= data[:months_should_pay] %></div>
                  <div class="text-xs text-gray-500">bulan</div>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-center">
                  <% if data[:initial_arrears] > 0 %>
                    <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-orange-100 text-orange-800">
                      <%= data[:initial_arrears] %> bulan
                    </span>
                  <% else %>
                    <span class="text-sm text-gray-400">-</span>
                  <% end %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-center">
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium <%= data[:arrears_months] >= 6 ? 'bg-red-100 text-red-800' : 'bg-yellow-100 text-yellow-800' %>">
                    <%= data[:arrears_months] %> bulan
                  </span>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-center">
                  <% if data[:arrears_months] >= 6 %>
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
                      <svg class="mr-1 h-4 w-4" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
                      </svg>
                      Kritis
                    </span>
                  <% else %>
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                      <svg class="mr-1 h-4 w-4" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                      </svg>
                      Perhatian
                    </span>
                  <% end %>
                </td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <!-- Mobile Card View (hidden on desktop) -->
    <div class="md:hidden space-y-3">
      <% @arrears_data.each do |data| %>
        <div class="bg-white shadow rounded-lg overflow-hidden border-l-4 <%= data[:arrears_months] >= 6 ? 'border-red-500' : 'border-yellow-500' %>">
          <div class="px-3 py-3 sm:px-4 sm:py-5">
            <!-- Card Header -->
            <div class="flex items-start justify-between mb-3">
              <% if current_user.is_warga? %>
                <div>
                  <h3 class="text-2xl sm:text-3xl font-bold text-gray-900">
                    <%= data[:address].block_address.upcase %>
                  </h3>
                </div>
              <% else %>
                <div class="flex items-center min-w-0 flex-1">
                  <div class="flex-shrink-0 h-10 w-10 sm:h-12 sm:w-12">
                    <div class="h-10 w-10 sm:h-12 sm:w-12 rounded-full bg-indigo-100 flex items-center justify-center">
                      <span class="text-indigo-600 font-medium text-sm sm:text-base">
                        <%= data[:address].block_address.gsub(/[^A-Za-z]/, '').upcase %>
                      </span>
                    </div>
                  </div>
                  <div class="ml-2 sm:ml-3 min-w-0">
                    <h3 class="text-base sm:text-lg font-medium text-gray-900 truncate">
                      <%= data[:address].block_address.upcase %>
                    </h3>
                  </div>
                </div>
              <% end %>
              <% unless current_user.is_warga? %>
                <div class="flex-shrink-0 ml-2">
                  <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium <%= data[:arrears_months] >= 6 ? 'bg-red-100 text-red-800' : 'bg-yellow-100 text-yellow-800' %>">
                    <%= data[:arrears_months] %> bln
                  </span>
                </div>
              <% end %>
            </div>

            <!-- Card Body -->
            <div class="space-y-2 sm:space-y-3">
              <% if current_user.is_warga? %>
                <div class="flex items-center">
                  <svg class="h-4 w-4 sm:h-5 sm:w-5 text-gray-400 mr-2 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                  </svg>
                  <div class="flex-1">
                    <p class="text-sm font-medium text-gray-900">
                      <%= data[:head_of_family]&.name || '-' %>
                    </p>
                  </div>
                </div>
              <% else %>
                <div class="flex items-center">
                  <svg class="h-4 w-4 sm:h-5 sm:w-5 text-gray-400 mr-2 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                  </svg>
                  <div class="flex-1 min-w-0">
                    <p class="text-sm font-medium text-gray-900 truncate">
                      <%= data[:head_of_family]&.name || '-' %>
                    </p>
                    <% if data[:residents].count > 1 %>
                      <p class="text-xs text-gray-500">
                        + <%= data[:residents].count - 1 %> anggota
                      </p>
                    <% end %>
                  </div>
                </div>

                <div class="flex items-center">
                  <svg class="h-4 w-4 sm:h-5 sm:w-5 text-gray-400 mr-2 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                  </svg>
                  <p class="text-sm text-gray-700 truncate">
                    <%= data[:head_of_family]&.phone_number || '-' %>
                  </p>
                </div>

                <!-- Payment Details -->
                <div class="bg-gray-50 rounded-lg p-2 sm:p-3 space-y-1.5 sm:space-y-2">
                  <div class="flex justify-between items-center">
                    <span class="text-xs text-gray-600">Pembayaran 2025:</span>
                    <span class="text-sm font-medium text-gray-900"><%= data[:total_paid_2025] %> / <%= data[:months_should_pay] %> bulan</span>
                  </div>
                  <% if data[:initial_arrears] > 0 %>
                    <div class="flex justify-between items-center">
                      <span class="text-xs text-gray-600">Tunggakan Lama:</span>
                      <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-orange-100 text-orange-800">
                        <%= data[:initial_arrears] %> bulan
                      </span>
                    </div>
                  <% end %>
                  <div class="flex justify-between items-center pt-2 border-t border-gray-200">
                    <span class="text-xs font-medium text-gray-700">Total Bulan Tunggakan:</span>
                    <span class="text-sm font-bold <%= data[:arrears_months] >= 6 ? 'text-red-600' : 'text-yellow-600' %>">
                      <%= data[:arrears_months] %> bulan
                    </span>
                  </div>
                </div>

                <div class="flex items-center pt-2">
                  <% if data[:arrears_months] >= 6 %>
                    <span class="inline-flex items-center px-2.5 py-1 rounded-full text-xs font-medium bg-red-100 text-red-800">
                      <svg class="mr-1.5 h-4 w-4" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
                      </svg>
                      Status Kritis - Perlu Tindakan Segera
                    </span>
                  <% else %>
                    <span class="inline-flex items-center px-2.5 py-1 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                      <svg class="mr-1.5 h-4 w-4" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                      </svg>
                      Perlu Perhatian
                    </span>
                  <% end %>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>

  <!-- Summary Footer -->
  <% if @arrears_data.any? %>
    <div class="mt-3 sm:mt-6 bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-50 border border-blue-200 rounded-xl p-3 sm:p-5 shadow-sm">
      <div class="flex items-center mb-4">
        <div class="w-8 h-8 bg-blue-500 rounded-lg flex items-center justify-center mr-3">
          <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
          </svg>
        </div>
        <h3 class="text-base font-semibold text-gray-900">Ringkasan Data</h3>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
        <div class="bg-white rounded-lg p-4 shadow-sm border border-gray-100">
          <div class="flex items-center justify-between">
            <div class="flex-1">
              <div class="text-xs font-medium text-gray-500 uppercase tracking-wide mb-1">Periode Data</div>
              <div class="text-sm font-bold text-gray-900"><%= @start_date.strftime('%b %Y') %> - <%= @cutoff_date.strftime('%b %Y') %></div>
            </div>
            <div class="w-10 h-10 bg-blue-100 rounded-lg flex items-center justify-center">
              <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
              </svg>
            </div>
          </div>
        </div>

        <% unless current_user.is_warga? %>
          <div class="bg-white rounded-lg p-4 shadow-sm border border-gray-100">
            <div class="flex items-center justify-between">
              <div class="flex-1">
                <div class="text-xs font-medium text-gray-500 uppercase tracking-wide mb-1">Total Rumah</div>
                <div class="text-sm font-bold text-red-600"><%= @total_arrears_count %> rumah</div>
              </div>
              <div class="w-10 h-10 bg-red-100 rounded-lg flex items-center justify-center">
                <svg class="w-5 h-5 text-red-600" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M10.707 2.293a1 1 0 00-1.414 0l-7 7a1 1 0 001.414 1.414L4 10.414V17a1 1 0 001 1h2a1 1 0 001-1v-2a1 1 0 011-1h2a1 1 0 011 1v2a1 1 0 001 1h2a1 1 0 001-1v-6.586l.293.293a1 1 0 001.414-1.414l-7-7z"/>
                </svg>
              </div>
            </div>
          </div>
        <% end %>

        <div class="bg-white rounded-lg p-4 shadow-sm border border-gray-100">
          <div class="flex items-center justify-between">
            <div class="flex-1">
              <div class="text-xs font-medium text-gray-500 uppercase tracking-wide mb-1">Kriteria</div>
              <div class="text-sm font-bold text-amber-600">≥ <%= @threshold %> bulan</div>
            </div>
            <div class="w-10 h-10 bg-amber-100 rounded-lg flex items-center justify-center">
              <svg class="w-5 h-5 text-amber-600" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"/>
              </svg>
            </div>
          </div>
        </div>
      </div>

      <% if current_user.is_warga? %>
        <div class="mt-4 p-3 bg-white rounded-lg border border-blue-200">
          <div class="flex items-start space-x-2">
            <svg class="w-5 h-5 text-blue-500 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
            </svg>
            <div class="flex-1">
              <p class="text-xs text-gray-600 leading-relaxed">
                Data ini menampilkan informasi rumah yang memiliki tunggakan iuran.
                Untuk informasi lebih detail, silakan hubungi pengurus RT/RW setempat.
              </p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
