<div class="row">
  <div class="col-md-12 grid-margin stretch-card">
    <div class="card">
      <div class="card-body">
        <div class="d-sm-flex align-items-center mb-4">
          <h4 class="card-title mb-sm-0">App Setting Detail</h4>
        </div>
          <div class="table-responsive rounded p-1">
            <table class="table">
              <thead></thead>
              <tbody>
              <tr>
                <td class="font-weight-bold">ID</td>
                <td><%= @app_setting.id %></td>
              </tr>
              <tr>
                <td class="font-weight-bold">Home Page Text</td>
                <td><%= @app_setting.home_page_text %></td>
              </tr>
              <tr>
                <td colspan="2">
                  <%= link_to 'Edit', edit_app_setting_path(@app_setting), class: 'btn btn-primary mr-2' if current_user.role == 2 %>
                  <%= link_to 'Back', app_settings_path, class: 'btn btn-light'  %>
                </td>
              </tr>
              </tbody>
            </table>
          </div>
      </div>
    </div>
  </div>
</div>

