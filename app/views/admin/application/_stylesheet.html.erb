<%# Override Administrate's stylesheet to avoid SassC compilation errors %>
<%# We'll use basic Tailwind classes instead of Administrate's default styles %>
<style>
  /* Basic admin styling using Tailwind-compatible CSS */
  .main-content { padding: 1rem; }
  .header { background: #f9fafb; padding: 1rem; border-bottom: 1px solid #e5e7eb; }
  .table { width: 100%; border-collapse: collapse; }
  .table th, .table td { padding: 0.5rem; border: 1px solid #e5e7eb; }
  .table th { background: #f9fafb; font-weight: 600; }
  .btn { padding: 0.5rem 1rem; border-radius: 0.375rem; text-decoration: none; }
  .btn-primary { background: #3b82f6; color: white; }
  .btn-secondary { background: #6b7280; color: white; }
  .form-control { padding: 0.5rem; border: 1px solid #d1d5db; border-radius: 0.375rem; width: 100%; }
</style>