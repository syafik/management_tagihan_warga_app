<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>Solid Queue Dashboard - PuriAyana App</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Load Solid Queue Dashboard CSS -->
  <%= stylesheet_link_tag "solid_queue_dashboard/application", "data-turbo-track": "reload" %>

  <!-- Additional custom styles for better integration -->
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background-color: #f8fafc;
      margin: 0;
      padding: 0;
    }

    .header {
      background: #fff;
      border-bottom: 1px solid #e2e8f0;
      padding: 1rem 2rem;
      box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
    }

    .header h1 {
      margin: 0;
      color: #1a202c;
      font-size: 1.5rem;
      font-weight: 600;
    }

    .nav-links {
      margin-top: 1rem;
    }

    .nav-links a {
      display: inline-block;
      padding: 0.5rem 1rem;
      margin-right: 1rem;
      background: #e2e8f0;
      color: #4a5568;
      text-decoration: none;
      border-radius: 0.375rem;
      font-size: 0.875rem;
      transition: all 0.2s;
    }

    .nav-links a:hover {
      background: #cbd5e0;
      color: #2d3748;
    }

    .main-content {
      padding: 2rem;
      max-width: 1200px;
      margin: 0 auto;
    }

    /* Override any conflicting styles from main app */
    .table {
      width: 100%;
      margin: 1rem 0;
      border-collapse: collapse;
      background: #fff;
      border-radius: 0.5rem;
      overflow: hidden;
      box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
    }

    .table th {
      background: #f7fafc;
      font-weight: 600;
      color: #4a5568;
      padding: 0.75rem;
      text-align: left;
      border-bottom: 1px solid #e2e8f0;
    }

    .table td {
      padding: 0.75rem;
      border-bottom: 1px solid #f1f5f9;
      color: #2d3748;
    }
  </style>

  <link rel="shortcut icon" href="<%= asset_path('img_305218.png') %>" />
</head>
<body>
  <div class="header">
    <h1>Background Jobs Dashboard</h1>
    <div class="nav-links">
      <%= link_to "Back to Admin", "/admin" %>
      <%= link_to "Back to App", "/" %>
      <% if defined?(user_signed_in?) && user_signed_in? %>
        <%= link_to "Logout", "/users/sign_out", method: :delete, data: { turbo_method: :delete } %>
      <% end %>
    </div>
  </div>

  <div class="main-content">
    <%= yield %>
  </div>

  <%= javascript_include_tag "solid_queue_dashboard/application", "data-turbo-track": "reload" %>
</body>
</html>