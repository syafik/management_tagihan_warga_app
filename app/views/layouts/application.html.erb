<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-50">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>PuriAyana App!</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <!-- Vite assets -->
  <%= vite_client_tag %>
  <%= vite_stylesheet_tag 'application' %>
  <%= vite_javascript_tag 'application' %>
  
  <!-- CKEditor -->
  <%= javascript_include_tag Ckeditor.cdn_url %>
  
  <link rel="shortcut icon" href="<%= asset_path('img_305218.png') %>" />
</head>
<body class="h-full">
  <div class="min-h-full" data-controller="mobile-menu">
    <!-- Navigation -->
    <%= render partial: 'layouts/navbar' %>
    
    <div class="flex min-h-screen">
      <!-- Sidebar -->
      <%= render partial: 'layouts/sidebar' %>
      
      <!-- Main content wrapper -->
      <main class="flex-1 lg:ml-64 flex flex-col">
        <!-- Main content area -->
        <div class="flex-1 px-4 sm:px-6 pt-20 pb-6 lg:pb-6 pb-20">
          <%= render partial: 'shared/flash_message' if !flash.empty? %>
          <%= yield %>
        </div>
        
        <!-- Footer - always at bottom, hidden on mobile -->
        <footer class="bg-white border-t border-gray-200 mt-auto hidden lg:block">
          <div class="max-w-7xl mx-auto py-4 px-4 sm:px-6 sm:flex sm:items-center sm:justify-between">
            <span class="text-gray-500 text-xs sm:text-sm block sm:inline">Copyright © 2020 PAD2 Solution. All rights reserved.</span>
            <span class="text-gray-500 text-xs sm:text-sm mt-2 sm:mt-0 block sm:inline">Hand-crafted & made with <i class="text-red-500">❤</i></span>
          </div>
        </footer>
      </main>
    </div>
    
    <!-- Bottom Navigation for Mobile -->
    <% if user_signed_in? %>
      <%= render partial: 'layouts/bottom_nav' %>
    <% end %>
  </div>
</body>
</html>